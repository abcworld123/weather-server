export interface ResDefault {
  success: boolean;
  data?: any;
}

// ! express default
export interface RequestData {
  params?: any;
  body?: any;
  query?: any;
}

export interface ResponseData {
  // data?: any;
}
